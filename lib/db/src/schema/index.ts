// No tables are currently defined. WeatherAxis reads live data from the
// Open-Meteo API and does not persist anything yet. Add table modules here and
// re-export them when persistence is needed.
export {};
